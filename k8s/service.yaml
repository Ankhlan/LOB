apiVersion: v1
kind: Service
metadata:
  name: cre-exchange
  labels:
    app: cre-exchange
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: cre-exchange
